interface:
  display_name: "Frontend User Copy"
  short_description: "Write clear, human-facing UI strings"
  default_prompt: "Use $frontend-user-copy to rewrite visible frontend text so it sounds clear, friendly, and user-facing."
